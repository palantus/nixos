
Rebuild:

``` bash
sudo nixos-rebuild switch --flake /home/ahk/nixos#thinkpad
sudo nixos-rebuild switch --flake /home/ahk/nixos#desktop
```

Based on this config: https://github.com/EmergentMind/nix-config
